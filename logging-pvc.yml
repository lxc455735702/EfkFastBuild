apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: logging-pvc
    namespace: logging
spec:
    storageClassName: logging-pv
    accessModes:
        - ReadWriteOnce
    resources:
        requests:
            storage: 10Gi
